$(document).ready(function(){
    console.log("loading Groups");

    /*for (group of groups){
		var info = getDisplayObject(group.displayName);
		console.log(group.displayName);

		var groupString = '<div class = "group"><icon style="background-color:rgb(' + info.r + ',' + info.g + ',' + info.b + ')"><h2>' + info.initials + '</h2></icon><h3>' + group.displayName + '</h3></div>';

		$("#groups").append(groupString);
	}*/

    console.log("loaded!");
})
